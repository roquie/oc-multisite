# ===================================
#  List Column Definitions
# ===================================

columns:
    domain:
        label: roquie.multisite::lang.tables.domain
        searchable: true

    theme:
        label: roquie.multisite::lang.tables.theme
        searchable: true

    is_protected:
        label: roquie.multisite::lang.tables.protected
        type: switch